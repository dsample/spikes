# Client-side data sharing approaches

## JSONP

JSONP is just a `script` tag which points to a JavaScript file hosted on the resource's domain. As long as the script is designed well (scoped variables).